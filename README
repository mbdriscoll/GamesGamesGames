games, for fun!

-----------------------------------------------------------

To install:
$ ./configure
$ make
$ make install

-----------------------------------------------------------
----NOTES--------------------------------------------------
-----------------------------------------------------------

I (Michael) needed this to configure:
CC=/usr/bin/gcc CXX=/usr/bin/g++ ./configure --x-includes=/usr/X11/include --x-libraries=/usr/X11/lib
